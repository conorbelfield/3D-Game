Shader.source[document.currentScript.src.split('js/shaders/')[1]] = `
	precision highp float;
	varying vec4 color; // ADDED
	uniform vec3 solidColor;
	uniform float time;

	void main(void) {
		gl_FragColor = color  * vec4(solidColor, 1) + sin(time)*.2;
	}
`;